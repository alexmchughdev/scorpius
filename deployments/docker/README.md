# Docker

Dockerfile and Compose examples for running the agent in a container. The
container needs `CAP_NET_ADMIN`, `CAP_SYS_ADMIN`, and access to the host
network namespace to apply most faults; that configuration goes here.
